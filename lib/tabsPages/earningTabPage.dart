import 'package:flutter/material.dart';

class EarningTabPage extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('this is Earning tab page'),
    );
  }
}
